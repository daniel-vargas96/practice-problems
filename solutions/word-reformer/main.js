function wordReformer(wordFragment1, wordFragment2) {
  return wordFragment1 + wordFragment2;
}

var wordReformerResult = wordReformer('bro', 'ken');
console.log('wordReformer function:', wordReformerResult);
